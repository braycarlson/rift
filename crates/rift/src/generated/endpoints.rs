// Generated by srcgen. Do not edit.
#![allow(clippy::pedantic)]

use std::borrow::Cow;

use crate::client::{Auth, RequestPlan, RiotApi, path_encode};
use crate::error::Error;

use super::routes::{PlatformRoute, RegionalRoute, ValPlatformRoute};

/// Optional query parameters for `lol_rso_match_v1_get_match_ids`.
#[derive(Clone, Copy, Debug, Default)]
pub struct LolRsoMatchV1GetMatchIdsQuery<'a> {
    /// Defaults to 20. Valid values: 0 to 100. Number of match ids to return.
    pub count: Option<i32>,
    /// Epoch timestamp in seconds.
    pub end_time: Option<i64>,
    /// Filter the list of match ids by a specific queue id. This filter is mutually inclusive of
    /// the type filter meaning any match ids returned must match both the queue and type filters.
    pub queue: Option<i32>,
    /// Defaults to 0. Start index.
    pub start: Option<i32>,
    /// Epoch timestamp in seconds. The matchlist started storing timestamps on June 16th, 2021. Any
    /// matches played before June 16th, 2021 won't be included in the results if the startTime
    /// filter is set.
    pub start_time: Option<i64>,
    /// Filter the list of match ids by the type of match. This filter is mutually inclusive of the
    /// queue filter meaning any match ids returned must match both the queue and type filters.
    pub r#type: Option<&'a str>,
}

/// Optional query parameters for `match_v5_get_match_ids_by_puuid`.
#[derive(Clone, Copy, Debug, Default)]
pub struct MatchV5GetMatchIdsByPuuidQuery<'a> {
    /// Defaults to 20. Valid values: 0 to 100. Number of match ids to return.
    pub count: Option<i32>,
    /// Epoch timestamp in seconds.
    pub end_time: Option<i64>,
    /// Filter the list of match ids by a specific queue id. This filter is mutually inclusive of
    /// the type filter meaning any match ids returned must match both the queue and type filters.
    pub queue: Option<i32>,
    /// Defaults to 0. Start index.
    pub start: Option<i32>,
    /// Epoch timestamp in seconds. The matchlist started storing timestamps on June 16th, 2021. Any
    /// matches played before June 16th, 2021 won't be included in the results if the startTime
    /// filter is set.
    pub start_time: Option<i64>,
    /// Filter the list of match ids by the type of match. This filter is mutually inclusive of the
    /// queue filter meaning any match ids returned must match both the queue and type filters.
    pub r#type: Option<&'a str>,
}

/// Optional query parameters for `tft_match_v1_get_match_ids_by_puuid`.
#[derive(Clone, Copy, Debug, Default)]
pub struct TftMatchV1GetMatchIdsByPuuidQuery {
    /// Defaults to 20. Number of match ids to return.
    pub count: Option<i32>,
    /// Epoch timestamp in seconds.
    pub end_time: Option<i64>,
    /// Defaults to 0. Start index.
    pub start: Option<i32>,
    /// Epoch timestamp in seconds. The matchlist started storing timestamps on June 16th, 2021. Any
    /// matches played before June 16th, 2021 won't be included in the results if the startTime
    /// filter is set.
    pub start_time: Option<i64>,
}

impl RiotApi {
    /// Get active region (lol and tft)
    ///
    /// # Riot API reference
    ///
    /// Operation `account-v1.getActiveRegion`:
    /// <https://developer.riotgames.com/api-methods/#account-v1/GET_getActiveRegion>
    pub async fn account_v1_get_active_region(
        &self,
        route: RegionalRoute,
        game: &str,
        puuid: &str,
    ) -> Result<super::models::account_v1::AccountRegionDto, Error> {
        assert!(!game.is_empty(), "game must not be empty");
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!(
            "/riot/account/v1/region/by-game/{}/by-puuid/{}",
            path_encode(game),
            path_encode(puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "account-v1.getActiveRegion",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get active shard for a player
    ///
    /// # Riot API reference
    ///
    /// Operation `account-v1.getActiveShard`:
    /// <https://developer.riotgames.com/api-methods/#account-v1/GET_getActiveShard>
    pub async fn account_v1_get_active_shard(
        &self,
        route: RegionalRoute,
        game: &str,
        puuid: &str,
    ) -> Result<Option<super::models::account_v1::ActiveShardDto>, Error> {
        assert!(!game.is_empty(), "game must not be empty");
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!(
            "/riot/account/v1/active-shards/by-game/{}/by-puuid/{}",
            path_encode(game),
            path_encode(puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "account-v1.getActiveShard",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get account by access token
    ///
    /// # Riot API reference
    ///
    /// Operation `account-v1.getByAccessToken`:
    /// <https://developer.riotgames.com/api-methods/#account-v1/GET_getByAccessToken>
    pub async fn account_v1_get_by_access_token(
        &self,
        access_token: &str,
        route: RegionalRoute,
    ) -> Result<super::models::account_v1::AccountDto, Error> {
        assert!(!access_token.is_empty(), "access token must not be empty");

        let path = Cow::Borrowed("/riot/account/v1/accounts/me");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::Bearer(access_token.to_string()),
            body: None,
            endpoint_id: "account-v1.getByAccessToken",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get account by puuid
    ///
    /// # Riot API reference
    ///
    /// Operation `account-v1.getByPuuid`:
    /// <https://developer.riotgames.com/api-methods/#account-v1/GET_getByPuuid>
    pub async fn account_v1_get_by_puuid(
        &self,
        route: RegionalRoute,
        puuid: &str,
    ) -> Result<super::models::account_v1::AccountDto, Error> {
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!(
            "/riot/account/v1/accounts/by-puuid/{}",
            path_encode(puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "account-v1.getByPuuid",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get account by riot id
    ///
    /// # Riot API reference
    ///
    /// Operation `account-v1.getByRiotId`:
    /// <https://developer.riotgames.com/api-methods/#account-v1/GET_getByRiotId>
    pub async fn account_v1_get_by_riot_id(
        &self,
        route: RegionalRoute,
        game_name: &str,
        tag_line: &str,
    ) -> Result<Option<super::models::account_v1::AccountDto>, Error> {
        assert!(!game_name.is_empty(), "game name must not be empty");
        assert!(!tag_line.is_empty(), "tag line must not be empty");

        let path = Cow::Owned(format!(
            "/riot/account/v1/accounts/by-riot-id/{}/{}",
            path_encode(game_name),
            path_encode(tag_line)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "account-v1.getByRiotId",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get all champion mastery entries sorted by number of champion points descending.
    ///
    /// # Riot API reference
    ///
    /// Operation `champion-mastery-v4.getAllChampionMasteriesByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#champion-mastery-v4/GET_getAllChampionMasteriesByPUUID>
    pub async fn champion_mastery_v4_get_all_champion_masteries_by_puuid(
        &self,
        route: PlatformRoute,
        encrypted_puuid: &str,
    ) -> Result<Vec<super::models::champion_mastery_v4::ChampionMasteryDto>, Error> {
        assert!(
            !encrypted_puuid.is_empty(),
            "encrypted puuid must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/champion-mastery/v4/champion-masteries/by-puuid/{}",
            path_encode(encrypted_puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "champion-mastery-v4.getAllChampionMasteriesByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a champion mastery by puuid and champion ID.
    ///
    /// # Riot API reference
    ///
    /// Operation `champion-mastery-v4.getChampionMasteryByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#champion-mastery-v4/GET_getChampionMasteryByPUUID>
    pub async fn champion_mastery_v4_get_champion_mastery_by_puuid(
        &self,
        route: PlatformRoute,
        encrypted_puuid: &str,
        champion_id: i32,
    ) -> Result<super::models::champion_mastery_v4::ChampionMasteryDto, Error> {
        assert!(
            !encrypted_puuid.is_empty(),
            "encrypted puuid must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/champion-mastery/v4/champion-masteries/by-puuid/{}/by-champion/{}",
            path_encode(encrypted_puuid),
            champion_id
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "champion-mastery-v4.getChampionMasteryByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a player's total champion mastery score, which is the sum of individual champion mastery
    /// levels.
    ///
    /// # Riot API reference
    ///
    /// Operation `champion-mastery-v4.getChampionMasteryScoreByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#champion-mastery-v4/GET_getChampionMasteryScoreByPUUID>
    pub async fn champion_mastery_v4_get_champion_mastery_score_by_puuid(
        &self,
        route: PlatformRoute,
        encrypted_puuid: &str,
    ) -> Result<i32, Error> {
        assert!(
            !encrypted_puuid.is_empty(),
            "encrypted puuid must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/champion-mastery/v4/scores/by-puuid/{}",
            path_encode(encrypted_puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "champion-mastery-v4.getChampionMasteryScoreByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get specified number of top champion mastery entries sorted by number of champion points
    /// descending.
    ///
    /// # Riot API reference
    ///
    /// Operation `champion-mastery-v4.getTopChampionMasteriesByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#champion-mastery-v4/GET_getTopChampionMasteriesByPUUID>
    pub async fn champion_mastery_v4_get_top_champion_masteries_by_puuid(
        &self,
        route: PlatformRoute,
        encrypted_puuid: &str,
        count: Option<i32>,
    ) -> Result<Vec<super::models::champion_mastery_v4::ChampionMasteryDto>, Error> {
        assert!(
            !encrypted_puuid.is_empty(),
            "encrypted puuid must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/champion-mastery/v4/champion-masteries/by-puuid/{}/top",
            path_encode(encrypted_puuid)
        ));

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(1);

        if let Some(value) = count {
            query_pairs.push(("count", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "champion-mastery-v4.getTopChampionMasteriesByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Returns champion rotations, including free-to-play and low-level free-to-play rotations
    /// (REST)
    ///
    /// # Riot API reference
    ///
    /// Operation `champion-v3.getChampionInfo`:
    /// <https://developer.riotgames.com/api-methods/#champion-v3/GET_getChampionInfo>
    pub async fn champion_v3_get_champion_info(
        &self,
        route: PlatformRoute,
    ) -> Result<super::models::champion_v3::ChampionInfo, Error> {
        let path = Cow::Borrowed("/lol/platform/v3/champion-rotations");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "champion-v3.getChampionInfo",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get players by puuid
    /// ## Implementation Notes
    /// This endpoint returns a list of active Clash players for a given PUUID. If a summoner
    /// registers for multiple tournaments at the same time (e.g., Saturday and Sunday) then both
    /// registrations would appear in this list.
    ///
    /// # Riot API reference
    ///
    /// Operation `clash-v1.getPlayersByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#clash-v1/GET_getPlayersByPUUID>
    pub async fn clash_v1_get_players_by_puuid(
        &self,
        route: PlatformRoute,
        puuid: &str,
    ) -> Result<Vec<super::models::clash_v1::PlayerDto>, Error> {
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!(
            "/lol/clash/v1/players/by-puuid/{}",
            path_encode(puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "clash-v1.getPlayersByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get team by ID.
    ///
    /// # Riot API reference
    ///
    /// Operation `clash-v1.getTeamById`:
    /// <https://developer.riotgames.com/api-methods/#clash-v1/GET_getTeamById>
    pub async fn clash_v1_get_team_by_id(
        &self,
        route: PlatformRoute,
        team_id: &str,
    ) -> Result<Option<super::models::clash_v1::TeamDto>, Error> {
        assert!(!team_id.is_empty(), "team id must not be empty");

        let path = Cow::Owned(format!("/lol/clash/v1/teams/{}", path_encode(team_id)));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "clash-v1.getTeamById",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get tournament by ID.
    ///
    /// # Riot API reference
    ///
    /// Operation `clash-v1.getTournamentById`:
    /// <https://developer.riotgames.com/api-methods/#clash-v1/GET_getTournamentById>
    pub async fn clash_v1_get_tournament_by_id(
        &self,
        route: PlatformRoute,
        tournament_id: i32,
    ) -> Result<Option<super::models::clash_v1::TournamentDto>, Error> {
        let path = Cow::Owned(format!("/lol/clash/v1/tournaments/{}", tournament_id));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "clash-v1.getTournamentById",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get tournament by team ID.
    ///
    /// # Riot API reference
    ///
    /// Operation `clash-v1.getTournamentByTeam`:
    /// <https://developer.riotgames.com/api-methods/#clash-v1/GET_getTournamentByTeam>
    pub async fn clash_v1_get_tournament_by_team(
        &self,
        route: PlatformRoute,
        team_id: &str,
    ) -> Result<Option<super::models::clash_v1::TournamentDto>, Error> {
        assert!(!team_id.is_empty(), "team id must not be empty");

        let path = Cow::Owned(format!(
            "/lol/clash/v1/tournaments/by-team/{}",
            path_encode(team_id)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "clash-v1.getTournamentByTeam",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get all active or upcoming tournaments.
    ///
    /// # Riot API reference
    ///
    /// Operation `clash-v1.getTournaments`:
    /// <https://developer.riotgames.com/api-methods/#clash-v1/GET_getTournaments>
    pub async fn clash_v1_get_tournaments(
        &self,
        route: PlatformRoute,
    ) -> Result<Vec<super::models::clash_v1::TournamentDto>, Error> {
        let path = Cow::Borrowed("/lol/clash/v1/tournaments");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "clash-v1.getTournaments",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get all the league entries.
    ///
    /// # Riot API reference
    ///
    /// Operation `league-exp-v4.getLeagueEntries`:
    /// <https://developer.riotgames.com/api-methods/#league-exp-v4/GET_getLeagueEntries>
    pub async fn league_exp_v4_get_league_entries(
        &self,
        route: PlatformRoute,
        queue: &str,
        tier: &str,
        division: &str,
        page: Option<i32>,
    ) -> Result<Option<Vec<super::models::league_exp_v4::LeagueEntryDto>>, Error> {
        assert!(!queue.is_empty(), "queue must not be empty");
        assert!(!tier.is_empty(), "tier must not be empty");
        assert!(!division.is_empty(), "division must not be empty");

        let path = Cow::Owned(format!(
            "/lol/league-exp/v4/entries/{}/{}/{}",
            path_encode(queue),
            path_encode(tier),
            path_encode(division)
        ));

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(1);

        if let Some(value) = page {
            query_pairs.push(("page", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "league-exp-v4.getLeagueEntries",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get the challenger league for given queue.
    ///
    /// # Riot API reference
    ///
    /// Operation `league-v4.getChallengerLeague`:
    /// <https://developer.riotgames.com/api-methods/#league-v4/GET_getChallengerLeague>
    pub async fn league_v4_get_challenger_league(
        &self,
        route: PlatformRoute,
        queue: &str,
    ) -> Result<super::models::league_v4::LeagueListDto, Error> {
        assert!(!queue.is_empty(), "queue must not be empty");

        let path = Cow::Owned(format!(
            "/lol/league/v4/challengerleagues/by-queue/{}",
            path_encode(queue)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "league-v4.getChallengerLeague",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get the grandmaster league of a specific queue.
    ///
    /// # Riot API reference
    ///
    /// Operation `league-v4.getGrandmasterLeague`:
    /// <https://developer.riotgames.com/api-methods/#league-v4/GET_getGrandmasterLeague>
    pub async fn league_v4_get_grandmaster_league(
        &self,
        route: PlatformRoute,
        queue: &str,
    ) -> Result<super::models::league_v4::LeagueListDto, Error> {
        assert!(!queue.is_empty(), "queue must not be empty");

        let path = Cow::Owned(format!(
            "/lol/league/v4/grandmasterleagues/by-queue/{}",
            path_encode(queue)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "league-v4.getGrandmasterLeague",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get league with given ID, including inactive entries.
    /// ## Implementation Notes
    /// Consistently looking up league ids that don't exist will result in a blacklist.
    ///
    /// # Riot API reference
    ///
    /// Operation `league-v4.getLeagueById`:
    /// <https://developer.riotgames.com/api-methods/#league-v4/GET_getLeagueById>
    pub async fn league_v4_get_league_by_id(
        &self,
        route: PlatformRoute,
        league_id: &str,
    ) -> Result<super::models::league_v4::LeagueListDto, Error> {
        assert!(!league_id.is_empty(), "league id must not be empty");

        let path = Cow::Owned(format!("/lol/league/v4/leagues/{}", path_encode(league_id)));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "league-v4.getLeagueById",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get all the league entries.
    ///
    /// # Riot API reference
    ///
    /// Operation `league-v4.getLeagueEntries`:
    /// <https://developer.riotgames.com/api-methods/#league-v4/GET_getLeagueEntries>
    pub async fn league_v4_get_league_entries(
        &self,
        route: PlatformRoute,
        queue: &str,
        tier: &str,
        division: &str,
        page: Option<i32>,
    ) -> Result<Option<Vec<super::models::league_v4::LeagueEntryDto>>, Error> {
        assert!(!queue.is_empty(), "queue must not be empty");
        assert!(!tier.is_empty(), "tier must not be empty");
        assert!(!division.is_empty(), "division must not be empty");

        let path = Cow::Owned(format!(
            "/lol/league/v4/entries/{}/{}/{}",
            path_encode(queue),
            path_encode(tier),
            path_encode(division)
        ));

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(1);

        if let Some(value) = page {
            query_pairs.push(("page", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "league-v4.getLeagueEntries",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get league entries in all queues for a given puuid
    ///
    /// # Riot API reference
    ///
    /// Operation `league-v4.getLeagueEntriesByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#league-v4/GET_getLeagueEntriesByPUUID>
    pub async fn league_v4_get_league_entries_by_puuid(
        &self,
        route: PlatformRoute,
        encrypted_puuid: &str,
    ) -> Result<Vec<super::models::league_v4::LeagueEntryDto>, Error> {
        assert!(
            !encrypted_puuid.is_empty(),
            "encrypted puuid must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/league/v4/entries/by-puuid/{}",
            path_encode(encrypted_puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "league-v4.getLeagueEntriesByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get the master league for given queue.
    ///
    /// # Riot API reference
    ///
    /// Operation `league-v4.getMasterLeague`:
    /// <https://developer.riotgames.com/api-methods/#league-v4/GET_getMasterLeague>
    pub async fn league_v4_get_master_league(
        &self,
        route: PlatformRoute,
        queue: &str,
    ) -> Result<super::models::league_v4::LeagueListDto, Error> {
        assert!(!queue.is_empty(), "queue must not be empty");

        let path = Cow::Owned(format!(
            "/lol/league/v4/masterleagues/by-queue/{}",
            path_encode(queue)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "league-v4.getMasterLeague",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// List of all basic challenge configuration information (includes all translations for names
    /// and descriptions)
    ///
    /// # Riot API reference
    ///
    /// Operation `lol-challenges-v1.getAllChallengeConfigs`:
    /// <https://developer.riotgames.com/api-methods/#lol-challenges-v1/GET_getAllChallengeConfigs>
    pub async fn lol_challenges_v1_get_all_challenge_configs(
        &self,
        route: PlatformRoute,
    ) -> Result<Vec<super::models::lol_challenges_v1::ChallengeConfigInfoDto>, Error> {
        let path = Cow::Borrowed("/lol/challenges/v1/challenges/config");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lol-challenges-v1.getAllChallengeConfigs",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Map of level to percentile of players who have achieved it - keys: ChallengeId -> Season ->
    /// Level -> percentile of players who achieved it
    ///
    /// # Riot API reference
    ///
    /// Operation `lol-challenges-v1.getAllChallengePercentiles`:
    /// <https://developer.riotgames.com/api-methods/#lol-challenges-v1/GET_getAllChallengePercentiles>
    pub async fn lol_challenges_v1_get_all_challenge_percentiles(
        &self,
        route: PlatformRoute,
    ) -> Result<rustc_hash::FxHashMap<String, rustc_hash::FxHashMap<String, f64>>, Error> {
        let path = Cow::Borrowed("/lol/challenges/v1/challenges/percentiles");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lol-challenges-v1.getAllChallengePercentiles",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get challenge configuration (REST)
    ///
    /// # Riot API reference
    ///
    /// Operation `lol-challenges-v1.getChallengeConfigs`:
    /// <https://developer.riotgames.com/api-methods/#lol-challenges-v1/GET_getChallengeConfigs>
    pub async fn lol_challenges_v1_get_challenge_configs(
        &self,
        route: PlatformRoute,
        challenge_id: i64,
    ) -> Result<Option<super::models::lol_challenges_v1::ChallengeConfigInfoDto>, Error> {
        let path = Cow::Owned(format!(
            "/lol/challenges/v1/challenges/{}/config",
            challenge_id
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lol-challenges-v1.getChallengeConfigs",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Return top players for each level. Level must be MASTER, GRANDMASTER or CHALLENGER.
    ///
    /// # Riot API reference
    ///
    /// Operation `lol-challenges-v1.getChallengeLeaderboards`:
    /// <https://developer.riotgames.com/api-methods/#lol-challenges-v1/GET_getChallengeLeaderboards>
    pub async fn lol_challenges_v1_get_challenge_leaderboards(
        &self,
        route: PlatformRoute,
        challenge_id: i64,
        level: &str,
        limit: Option<i32>,
    ) -> Result<Option<Vec<super::models::lol_challenges_v1::ApexPlayerInfoDto>>, Error> {
        assert!(!level.is_empty(), "level must not be empty");

        let path = Cow::Owned(format!(
            "/lol/challenges/v1/challenges/{}/leaderboards/by-level/{}",
            challenge_id,
            path_encode(level)
        ));

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(1);

        if let Some(value) = limit {
            query_pairs.push(("limit", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lol-challenges-v1.getChallengeLeaderboards",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Map of level to percentile of players who have achieved it
    ///
    /// # Riot API reference
    ///
    /// Operation `lol-challenges-v1.getChallengePercentiles`:
    /// <https://developer.riotgames.com/api-methods/#lol-challenges-v1/GET_getChallengePercentiles>
    pub async fn lol_challenges_v1_get_challenge_percentiles(
        &self,
        route: PlatformRoute,
        challenge_id: i64,
    ) -> Result<Option<rustc_hash::FxHashMap<String, f64>>, Error> {
        let path = Cow::Owned(format!(
            "/lol/challenges/v1/challenges/{}/percentiles",
            challenge_id
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lol-challenges-v1.getChallengePercentiles",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Returns player information with list of all progressed challenges (REST)
    ///
    /// # Riot API reference
    ///
    /// Operation `lol-challenges-v1.getPlayerData`:
    /// <https://developer.riotgames.com/api-methods/#lol-challenges-v1/GET_getPlayerData>
    pub async fn lol_challenges_v1_get_player_data(
        &self,
        route: PlatformRoute,
        puuid: &str,
    ) -> Result<super::models::lol_challenges_v1::PlayerInfoDto, Error> {
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!(
            "/lol/challenges/v1/player-data/{}",
            path_encode(puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lol-challenges-v1.getPlayerData",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a match by match id
    ///
    /// # Riot API reference
    ///
    /// Operation `lol-rso-match-v1.getMatch`:
    /// <https://developer.riotgames.com/api-methods/#lol-rso-match-v1/GET_getMatch>
    pub async fn lol_rso_match_v1_get_match(
        &self,
        access_token: &str,
        route: RegionalRoute,
        match_id: &str,
    ) -> Result<super::models::match_v5::MatchDto, Error> {
        assert!(!access_token.is_empty(), "access token must not be empty");
        assert!(!match_id.is_empty(), "match id must not be empty");

        let path = Cow::Owned(format!(
            "/lol/rso-match/v1/matches/{}",
            path_encode(match_id)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::Bearer(access_token.to_string()),
            body: None,
            endpoint_id: "lol-rso-match-v1.getMatch",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a list of match ids by player access token - Includes custom matches
    ///
    /// # Riot API reference
    ///
    /// Operation `lol-rso-match-v1.getMatchIds`:
    /// <https://developer.riotgames.com/api-methods/#lol-rso-match-v1/GET_getMatchIds>
    pub async fn lol_rso_match_v1_get_match_ids(
        &self,
        access_token: &str,
        route: RegionalRoute,
        query: &LolRsoMatchV1GetMatchIdsQuery<'_>,
    ) -> Result<Vec<String>, Error> {
        assert!(!access_token.is_empty(), "access token must not be empty");

        let path = Cow::Borrowed("/lol/rso-match/v1/matches/ids");

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(6);

        if let Some(value) = query.count {
            query_pairs.push(("count", value.to_string()));
        }

        if let Some(value) = query.start {
            query_pairs.push(("start", value.to_string()));
        }

        if let Some(value) = query.r#type {
            query_pairs.push(("type", value.to_string()));
        }

        if let Some(value) = query.queue {
            query_pairs.push(("queue", value.to_string()));
        }

        if let Some(value) = query.end_time {
            query_pairs.push(("endTime", value.to_string()));
        }

        if let Some(value) = query.start_time {
            query_pairs.push(("startTime", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::Bearer(access_token.to_string()),
            body: None,
            endpoint_id: "lol-rso-match-v1.getMatchIds",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a match timeline by match id
    ///
    /// # Riot API reference
    ///
    /// Operation `lol-rso-match-v1.getTimeline`:
    /// <https://developer.riotgames.com/api-methods/#lol-rso-match-v1/GET_getTimeline>
    pub async fn lol_rso_match_v1_get_timeline(
        &self,
        access_token: &str,
        route: RegionalRoute,
        match_id: &str,
    ) -> Result<super::models::match_v5::TimelineDto, Error> {
        assert!(!access_token.is_empty(), "access token must not be empty");
        assert!(!match_id.is_empty(), "match id must not be empty");

        let path = Cow::Owned(format!(
            "/lol/rso-match/v1/matches/{}/timeline",
            path_encode(match_id)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::Bearer(access_token.to_string()),
            body: None,
            endpoint_id: "lol-rso-match-v1.getTimeline",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get League of Legends status for the given platform.
    ///
    /// # Riot API reference
    ///
    /// Operation `lol-status-v4.getPlatformData`:
    /// <https://developer.riotgames.com/api-methods/#lol-status-v4/GET_getPlatformData>
    pub async fn lol_status_v4_get_platform_data(
        &self,
        route: PlatformRoute,
    ) -> Result<super::models::lol_status_v4::PlatformDataDto, Error> {
        let path = Cow::Borrowed("/lol/status/v4/platform-data");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lol-status-v4.getPlatformData",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Create a new deck for the calling user.
    ///
    /// # Riot API reference
    ///
    /// Operation `lor-deck-v1.createDeck`:
    /// <https://developer.riotgames.com/api-methods/#lor-deck-v1/POST_createDeck>
    pub async fn lor_deck_v1_create_deck(
        &self,
        route: RegionalRoute,
        body: &super::models::lor_deck_v1::NewDeckDto,
    ) -> Result<String, Error> {
        let path = Cow::Borrowed("/lor/deck/v1/decks/me");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let body = serde_json::to_string(body).map_err(Error::body_serialize)?;

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: Some(body),
            endpoint_id: "lor-deck-v1.createDeck",
            method: "POST",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a list of the calling user's decks.
    ///
    /// # Riot API reference
    ///
    /// Operation `lor-deck-v1.getDecks`:
    /// <https://developer.riotgames.com/api-methods/#lor-deck-v1/GET_getDecks>
    pub async fn lor_deck_v1_get_decks(
        &self,
        route: RegionalRoute,
    ) -> Result<Vec<super::models::lor_deck_v1::DeckDto>, Error> {
        let path = Cow::Borrowed("/lor/deck/v1/decks/me");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lor-deck-v1.getDecks",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Return a list of cards owned by the calling user.
    ///
    /// # Riot API reference
    ///
    /// Operation `lor-inventory-v1.getCards`:
    /// <https://developer.riotgames.com/api-methods/#lor-inventory-v1/GET_getCards>
    pub async fn lor_inventory_v1_get_cards(
        &self,
        route: RegionalRoute,
    ) -> Result<Vec<super::models::lor_inventory_v1::CardDto>, Error> {
        let path = Cow::Borrowed("/lor/inventory/v1/cards/me");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lor-inventory-v1.getCards",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get match by id
    ///
    /// # Riot API reference
    ///
    /// Operation `lor-match-v1.getMatch`:
    /// <https://developer.riotgames.com/api-methods/#lor-match-v1/GET_getMatch>
    pub async fn lor_match_v1_get_match(
        &self,
        route: RegionalRoute,
        match_id: &str,
    ) -> Result<super::models::lor_match_v1::MatchDto, Error> {
        assert!(!match_id.is_empty(), "match id must not be empty");

        let path = Cow::Owned(format!("/lor/match/v1/matches/{}", path_encode(match_id)));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lor-match-v1.getMatch",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a list of match ids by PUUID
    ///
    /// # Riot API reference
    ///
    /// Operation `lor-match-v1.getMatchIdsByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#lor-match-v1/GET_getMatchIdsByPUUID>
    pub async fn lor_match_v1_get_match_ids_by_puuid(
        &self,
        route: RegionalRoute,
        puuid: &str,
    ) -> Result<Vec<String>, Error> {
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!(
            "/lor/match/v1/matches/by-puuid/{}/ids",
            path_encode(puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lor-match-v1.getMatchIdsByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get the players in Master tier.
    ///
    /// # Riot API reference
    ///
    /// Operation `lor-ranked-v1.getLeaderboards`:
    /// <https://developer.riotgames.com/api-methods/#lor-ranked-v1/GET_getLeaderboards>
    pub async fn lor_ranked_v1_get_leaderboards(
        &self,
        route: RegionalRoute,
    ) -> Result<super::models::lor_ranked_v1::LeaderboardDto, Error> {
        let path = Cow::Borrowed("/lor/ranked/v1/leaderboards");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lor-ranked-v1.getLeaderboards",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get Legends of Runeterra status for the given platform.
    ///
    /// # Riot API reference
    ///
    /// Operation `lor-status-v1.getPlatformData`:
    /// <https://developer.riotgames.com/api-methods/#lor-status-v1/GET_getPlatformData>
    pub async fn lor_status_v1_get_platform_data(
        &self,
        route: RegionalRoute,
    ) -> Result<super::models::lor_status_v1::PlatformDataDto, Error> {
        let path = Cow::Borrowed("/lor/status/v1/platform-data");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "lor-status-v1.getPlatformData",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a match by match id
    ///
    /// # Riot API reference
    ///
    /// Operation `match-v5.getMatch`:
    /// <https://developer.riotgames.com/api-methods/#match-v5/GET_getMatch>
    pub async fn match_v5_get_match(
        &self,
        route: RegionalRoute,
        match_id: &str,
    ) -> Result<Option<super::models::match_v5::MatchDto>, Error> {
        assert!(!match_id.is_empty(), "match id must not be empty");

        let path = Cow::Owned(format!("/lol/match/v5/matches/{}", path_encode(match_id)));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "match-v5.getMatch",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get a list of match ids by puuid
    ///
    /// # Riot API reference
    ///
    /// Operation `match-v5.getMatchIdsByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#match-v5/GET_getMatchIdsByPUUID>
    pub async fn match_v5_get_match_ids_by_puuid(
        &self,
        route: RegionalRoute,
        puuid: &str,
        query: &MatchV5GetMatchIdsByPuuidQuery<'_>,
    ) -> Result<Vec<String>, Error> {
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!(
            "/lol/match/v5/matches/by-puuid/{}/ids",
            path_encode(puuid)
        ));

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(6);

        if let Some(value) = query.start_time {
            query_pairs.push(("startTime", value.to_string()));
        }

        if let Some(value) = query.end_time {
            query_pairs.push(("endTime", value.to_string()));
        }

        if let Some(value) = query.queue {
            query_pairs.push(("queue", value.to_string()));
        }

        if let Some(value) = query.r#type {
            query_pairs.push(("type", value.to_string()));
        }

        if let Some(value) = query.start {
            query_pairs.push(("start", value.to_string()));
        }

        if let Some(value) = query.count {
            query_pairs.push(("count", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "match-v5.getMatchIdsByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get player replays
    ///
    /// # Riot API reference
    ///
    /// Operation `match-v5.getReplay`:
    /// <https://developer.riotgames.com/api-methods/#match-v5/GET_getReplay>
    pub async fn match_v5_get_replay(
        &self,
        route: RegionalRoute,
        puuid: &str,
    ) -> Result<super::models::match_v5::ReplayDto, Error> {
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!(
            "/lol/match/v5/matches/by-puuid/{}/replays",
            path_encode(puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "match-v5.getReplay",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a match timeline by match id
    ///
    /// # Riot API reference
    ///
    /// Operation `match-v5.getTimeline`:
    /// <https://developer.riotgames.com/api-methods/#match-v5/GET_getTimeline>
    pub async fn match_v5_get_timeline(
        &self,
        route: RegionalRoute,
        match_id: &str,
    ) -> Result<Option<super::models::match_v5::TimelineDto>, Error> {
        assert!(!match_id.is_empty(), "match id must not be empty");

        let path = Cow::Owned(format!(
            "/lol/match/v5/matches/{}/timeline",
            path_encode(match_id)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "match-v5.getTimeline",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get riftbound content
    ///
    /// # Riot API reference
    ///
    /// Operation `riftbound-content-v1.getContent`:
    /// <https://developer.riotgames.com/api-methods/#riftbound-content-v1/GET_getContent>
    pub async fn riftbound_content_v1_get_content(
        &self,
        route: RegionalRoute,
        locale: Option<&str>,
    ) -> Result<super::models::riftbound_content_v1::RiftboundContentDto, Error> {
        let path = Cow::Borrowed("/riftbound/content/v1/contents");

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(1);

        if let Some(value) = locale {
            query_pairs.push(("locale", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "riftbound-content-v1.getContent",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get current game information for the given puuid.
    ///
    /// # Riot API reference
    ///
    /// Operation `spectator-tft-v5.getCurrentGameInfoByPuuid`:
    /// <https://developer.riotgames.com/api-methods/#spectator-tft-v5/GET_getCurrentGameInfoByPuuid>
    pub async fn spectator_tft_v5_get_current_game_info_by_puuid(
        &self,
        route: PlatformRoute,
        encrypted_puuid: &str,
    ) -> Result<Option<super::models::spectator_tft_v5::CurrentGameInfo>, Error> {
        assert!(
            !encrypted_puuid.is_empty(),
            "encrypted puuid must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/spectator/tft/v5/active-games/by-puuid/{}",
            path_encode(encrypted_puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "spectator-tft-v5.getCurrentGameInfoByPuuid",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get current game information for the given puuid.
    ///
    /// # Riot API reference
    ///
    /// Operation `spectator-v5.getCurrentGameInfoByPuuid`:
    /// <https://developer.riotgames.com/api-methods/#spectator-v5/GET_getCurrentGameInfoByPuuid>
    pub async fn spectator_v5_get_current_game_info_by_puuid(
        &self,
        route: PlatformRoute,
        encrypted_puuid: &str,
    ) -> Result<Option<super::models::spectator_v5::CurrentGameInfo>, Error> {
        assert!(
            !encrypted_puuid.is_empty(),
            "encrypted puuid must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/spectator/v5/active-games/by-summoner/{}",
            path_encode(encrypted_puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "spectator-v5.getCurrentGameInfoByPuuid",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get a summoner by access token.
    ///
    /// # Riot API reference
    ///
    /// Operation `summoner-v4.getByAccessToken`:
    /// <https://developer.riotgames.com/api-methods/#summoner-v4/GET_getByAccessToken>
    pub async fn summoner_v4_get_by_access_token(
        &self,
        route: PlatformRoute,
    ) -> Result<super::models::summoner_v4::SummonerDto, Error> {
        let path = Cow::Borrowed("/lol/summoner/v4/summoners/me");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "summoner-v4.getByAccessToken",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a summoner by PUUID.
    ///
    /// # Riot API reference
    ///
    /// Operation `summoner-v4.getByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#summoner-v4/GET_getByPUUID>
    pub async fn summoner_v4_get_by_puuid(
        &self,
        route: PlatformRoute,
        encrypted_puuid: &str,
    ) -> Result<Option<super::models::summoner_v4::SummonerDto>, Error> {
        assert!(
            !encrypted_puuid.is_empty(),
            "encrypted puuid must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/summoner/v4/summoners/by-puuid/{}",
            path_encode(encrypted_puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "summoner-v4.getByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get the challenger league.
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-league-v1.getChallengerLeague`:
    /// <https://developer.riotgames.com/api-methods/#tft-league-v1/GET_getChallengerLeague>
    pub async fn tft_league_v1_get_challenger_league(
        &self,
        route: PlatformRoute,
        queue: Option<&str>,
    ) -> Result<super::models::tft_league_v1::LeagueListDto, Error> {
        let path = Cow::Borrowed("/tft/league/v1/challenger");

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(1);

        if let Some(value) = queue {
            query_pairs.push(("queue", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-league-v1.getChallengerLeague",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get the grandmaster league.
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-league-v1.getGrandmasterLeague`:
    /// <https://developer.riotgames.com/api-methods/#tft-league-v1/GET_getGrandmasterLeague>
    pub async fn tft_league_v1_get_grandmaster_league(
        &self,
        route: PlatformRoute,
        queue: Option<&str>,
    ) -> Result<super::models::tft_league_v1::LeagueListDto, Error> {
        let path = Cow::Borrowed("/tft/league/v1/grandmaster");

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(1);

        if let Some(value) = queue {
            query_pairs.push(("queue", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-league-v1.getGrandmasterLeague",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get all the league entries.
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-league-v1.getLeagueEntries`:
    /// <https://developer.riotgames.com/api-methods/#tft-league-v1/GET_getLeagueEntries>
    pub async fn tft_league_v1_get_league_entries(
        &self,
        route: PlatformRoute,
        tier: &str,
        division: &str,
        queue: Option<&str>,
        page: Option<i32>,
    ) -> Result<Vec<super::models::tft_league_v1::LeagueEntryDto>, Error> {
        assert!(!tier.is_empty(), "tier must not be empty");
        assert!(!division.is_empty(), "division must not be empty");

        let path = Cow::Owned(format!(
            "/tft/league/v1/entries/{}/{}",
            path_encode(tier),
            path_encode(division)
        ));

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(2);

        if let Some(value) = queue {
            query_pairs.push(("queue", value.to_string()));
        }

        if let Some(value) = page {
            query_pairs.push(("page", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-league-v1.getLeagueEntries",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get league entries in all queues for a given puuid
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-league-v1.getLeagueEntriesByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#tft-league-v1/GET_getLeagueEntriesByPUUID>
    pub async fn tft_league_v1_get_league_entries_by_puuid(
        &self,
        route: PlatformRoute,
        puuid: &str,
    ) -> Result<Vec<super::models::tft_league_v1::LeagueEntryDto>, Error> {
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!("/tft/league/v1/by-puuid/{}", path_encode(puuid)));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-league-v1.getLeagueEntriesByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get the master league.
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-league-v1.getMasterLeague`:
    /// <https://developer.riotgames.com/api-methods/#tft-league-v1/GET_getMasterLeague>
    pub async fn tft_league_v1_get_master_league(
        &self,
        route: PlatformRoute,
        queue: Option<&str>,
    ) -> Result<super::models::tft_league_v1::LeagueListDto, Error> {
        let path = Cow::Borrowed("/tft/league/v1/master");

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(1);

        if let Some(value) = queue {
            query_pairs.push(("queue", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-league-v1.getMasterLeague",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get the top rated ladder for given queue
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-league-v1.getTopRatedLadder`:
    /// <https://developer.riotgames.com/api-methods/#tft-league-v1/GET_getTopRatedLadder>
    pub async fn tft_league_v1_get_top_rated_ladder(
        &self,
        route: PlatformRoute,
        queue: &str,
    ) -> Result<Vec<super::models::tft_league_v1::TopRatedLadderEntryDto>, Error> {
        assert!(!queue.is_empty(), "queue must not be empty");

        let path = Cow::Owned(format!(
            "/tft/league/v1/rated-ladders/{}/top",
            path_encode(queue)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-league-v1.getTopRatedLadder",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a match by match id
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-match-v1.getMatch`:
    /// <https://developer.riotgames.com/api-methods/#tft-match-v1/GET_getMatch>
    pub async fn tft_match_v1_get_match(
        &self,
        route: RegionalRoute,
        match_id: &str,
    ) -> Result<Option<super::models::tft_match_v1::MatchDto>, Error> {
        assert!(!match_id.is_empty(), "match id must not be empty");

        let path = Cow::Owned(format!("/tft/match/v1/matches/{}", path_encode(match_id)));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-match-v1.getMatch",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get a list of match ids by PUUID
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-match-v1.getMatchIdsByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#tft-match-v1/GET_getMatchIdsByPUUID>
    pub async fn tft_match_v1_get_match_ids_by_puuid(
        &self,
        route: RegionalRoute,
        puuid: &str,
        query: &TftMatchV1GetMatchIdsByPuuidQuery,
    ) -> Result<Vec<String>, Error> {
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!(
            "/tft/match/v1/matches/by-puuid/{}/ids",
            path_encode(puuid)
        ));

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(4);

        if let Some(value) = query.start {
            query_pairs.push(("start", value.to_string()));
        }

        if let Some(value) = query.end_time {
            query_pairs.push(("endTime", value.to_string()));
        }

        if let Some(value) = query.start_time {
            query_pairs.push(("startTime", value.to_string()));
        }

        if let Some(value) = query.count {
            query_pairs.push(("count", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-match-v1.getMatchIdsByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get Teamfight Tactics status for the given platform.
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-status-v1.getPlatformData`:
    /// <https://developer.riotgames.com/api-methods/#tft-status-v1/GET_getPlatformData>
    pub async fn tft_status_v1_get_platform_data(
        &self,
        route: PlatformRoute,
    ) -> Result<super::models::tft_status_v1::PlatformDataDto, Error> {
        let path = Cow::Borrowed("/tft/status/v1/platform-data");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-status-v1.getPlatformData",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a summoner by access token.
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-summoner-v1.getByAccessToken`:
    /// <https://developer.riotgames.com/api-methods/#tft-summoner-v1/GET_getByAccessToken>
    pub async fn tft_summoner_v1_get_by_access_token(
        &self,
        route: PlatformRoute,
    ) -> Result<super::models::tft_summoner_v1::SummonerDto, Error> {
        let path = Cow::Borrowed("/tft/summoner/v1/summoners/me");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-summoner-v1.getByAccessToken",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get a summoner by PUUID.
    ///
    /// # Riot API reference
    ///
    /// Operation `tft-summoner-v1.getByPUUID`:
    /// <https://developer.riotgames.com/api-methods/#tft-summoner-v1/GET_getByPUUID>
    pub async fn tft_summoner_v1_get_by_puuid(
        &self,
        route: PlatformRoute,
        encrypted_puuid: &str,
    ) -> Result<super::models::tft_summoner_v1::SummonerDto, Error> {
        assert!(
            !encrypted_puuid.is_empty(),
            "encrypted puuid must not be empty"
        );

        let path = Cow::Owned(format!(
            "/tft/summoner/v1/summoners/by-puuid/{}",
            path_encode(encrypted_puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tft-summoner-v1.getByPUUID",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Create a tournament code for the given tournament - Stub method
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-stub-v5.createTournamentCode`:
    /// <https://developer.riotgames.com/api-methods/#tournament-stub-v5/POST_createTournamentCode>
    pub async fn tournament_stub_v5_create_tournament_code(
        &self,
        route: RegionalRoute,
        tournament_id: i64,
        count: Option<i32>,
        body: &super::models::tournament_stub_v5::TournamentCodeParametersV5,
    ) -> Result<Vec<String>, Error> {
        let path = Cow::Borrowed("/lol/tournament-stub/v5/codes");

        let mut query_pairs: Vec<(&'static str, String)> =
            vec![("tournamentId", tournament_id.to_string())];

        if let Some(value) = count {
            query_pairs.push(("count", value.to_string()));
        }

        let body = serde_json::to_string(body).map_err(Error::body_serialize)?;

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: Some(body),
            endpoint_id: "tournament-stub-v5.createTournamentCode",
            method: "POST",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Gets a list of lobby events by tournament code - Stub method
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-stub-v5.getLobbyEventsByCode`:
    /// <https://developer.riotgames.com/api-methods/#tournament-stub-v5/GET_getLobbyEventsByCode>
    pub async fn tournament_stub_v5_get_lobby_events_by_code(
        &self,
        route: RegionalRoute,
        tournament_code: &str,
    ) -> Result<super::models::tournament_stub_v5::LobbyEventV5DtoWrapper, Error> {
        assert!(
            !tournament_code.is_empty(),
            "tournament code must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/tournament-stub/v5/lobby-events/by-code/{}",
            path_encode(tournament_code)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tournament-stub-v5.getLobbyEventsByCode",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Returns the tournament code DTO associated with a tournament code string - Stub Method
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-stub-v5.getTournamentCode`:
    /// <https://developer.riotgames.com/api-methods/#tournament-stub-v5/GET_getTournamentCode>
    pub async fn tournament_stub_v5_get_tournament_code(
        &self,
        route: RegionalRoute,
        tournament_code: &str,
    ) -> Result<super::models::tournament_stub_v5::TournamentCodeV5Dto, Error> {
        assert!(
            !tournament_code.is_empty(),
            "tournament code must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/tournament-stub/v5/codes/{}",
            path_encode(tournament_code)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tournament-stub-v5.getTournamentCode",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Creates a tournament provider and returns its ID - Stub method
    /// ## Implementation Notes
    /// Providers will need to call this endpoint first to register their callback URL and their API
    /// key with the tournament system before any other tournament provider endpoints will work.
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-stub-v5.registerProviderData`:
    /// <https://developer.riotgames.com/api-methods/#tournament-stub-v5/POST_registerProviderData>
    pub async fn tournament_stub_v5_register_provider_data(
        &self,
        route: RegionalRoute,
        body: &super::models::tournament_stub_v5::ProviderRegistrationParametersV5,
    ) -> Result<i32, Error> {
        let path = Cow::Borrowed("/lol/tournament-stub/v5/providers");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let body = serde_json::to_string(body).map_err(Error::body_serialize)?;

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: Some(body),
            endpoint_id: "tournament-stub-v5.registerProviderData",
            method: "POST",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Creates a tournament and returns its ID - Stub method
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-stub-v5.registerTournament`:
    /// <https://developer.riotgames.com/api-methods/#tournament-stub-v5/POST_registerTournament>
    pub async fn tournament_stub_v5_register_tournament(
        &self,
        route: RegionalRoute,
        body: &super::models::tournament_stub_v5::TournamentRegistrationParametersV5,
    ) -> Result<i32, Error> {
        let path = Cow::Borrowed("/lol/tournament-stub/v5/tournaments");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let body = serde_json::to_string(body).map_err(Error::body_serialize)?;

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: Some(body),
            endpoint_id: "tournament-stub-v5.registerTournament",
            method: "POST",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Create a tournament code for the given tournament.
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-v5.createTournamentCode`:
    /// <https://developer.riotgames.com/api-methods/#tournament-v5/POST_createTournamentCode>
    pub async fn tournament_v5_create_tournament_code(
        &self,
        route: RegionalRoute,
        tournament_id: i64,
        count: Option<i32>,
        body: &super::models::tournament_v5::TournamentCodeParametersV5,
    ) -> Result<Vec<String>, Error> {
        let path = Cow::Borrowed("/lol/tournament/v5/codes");

        let mut query_pairs: Vec<(&'static str, String)> =
            vec![("tournamentId", tournament_id.to_string())];

        if let Some(value) = count {
            query_pairs.push(("count", value.to_string()));
        }

        let body = serde_json::to_string(body).map_err(Error::body_serialize)?;

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: Some(body),
            endpoint_id: "tournament-v5.createTournamentCode",
            method: "POST",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get games details
    /// ## Implementation Notes
    /// Additional endpoint to get tournament games. From this endpoint, you are able to get
    /// participants PUUID (the callback doesn't contain this info).
    ///
    /// You can also use it to check if the game was recorded and validate callbacks. If the
    /// endpoint returns the game, it means a callback was attempted.
    ///
    /// This will only work for tournament codes created after November 10, 2023.
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-v5.getGames`:
    /// <https://developer.riotgames.com/api-methods/#tournament-v5/GET_getGames>
    pub async fn tournament_v5_get_games(
        &self,
        route: RegionalRoute,
        tournament_code: &str,
    ) -> Result<Vec<super::models::tournament_v5::TournamentGamesV5>, Error> {
        assert!(
            !tournament_code.is_empty(),
            "tournament code must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/tournament/v5/games/by-code/{}",
            path_encode(tournament_code)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tournament-v5.getGames",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Gets a list of lobby events by tournament code.
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-v5.getLobbyEventsByCode`:
    /// <https://developer.riotgames.com/api-methods/#tournament-v5/GET_getLobbyEventsByCode>
    pub async fn tournament_v5_get_lobby_events_by_code(
        &self,
        route: RegionalRoute,
        tournament_code: &str,
    ) -> Result<super::models::tournament_v5::LobbyEventV5DtoWrapper, Error> {
        assert!(
            !tournament_code.is_empty(),
            "tournament code must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/tournament/v5/lobby-events/by-code/{}",
            path_encode(tournament_code)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tournament-v5.getLobbyEventsByCode",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Returns the tournament code DTO associated with a tournament code string.
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-v5.getTournamentCode`:
    /// <https://developer.riotgames.com/api-methods/#tournament-v5/GET_getTournamentCode>
    pub async fn tournament_v5_get_tournament_code(
        &self,
        route: RegionalRoute,
        tournament_code: &str,
    ) -> Result<super::models::tournament_v5::TournamentCodeV5Dto, Error> {
        assert!(
            !tournament_code.is_empty(),
            "tournament code must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/tournament/v5/codes/{}",
            path_encode(tournament_code)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "tournament-v5.getTournamentCode",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Creates a tournament provider and returns its ID.
    /// ## Implementation Notes
    /// Providers will need to call this endpoint first to register their callback URL and their API
    /// key with the tournament system before any other tournament provider endpoints will work.
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-v5.registerProviderData`:
    /// <https://developer.riotgames.com/api-methods/#tournament-v5/POST_registerProviderData>
    pub async fn tournament_v5_register_provider_data(
        &self,
        route: RegionalRoute,
        body: &super::models::tournament_v5::ProviderRegistrationParametersV5,
    ) -> Result<i32, Error> {
        let path = Cow::Borrowed("/lol/tournament/v5/providers");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let body = serde_json::to_string(body).map_err(Error::body_serialize)?;

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: Some(body),
            endpoint_id: "tournament-v5.registerProviderData",
            method: "POST",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Creates a tournament and returns its ID.
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-v5.registerTournament`:
    /// <https://developer.riotgames.com/api-methods/#tournament-v5/POST_registerTournament>
    pub async fn tournament_v5_register_tournament(
        &self,
        route: RegionalRoute,
        body: &super::models::tournament_v5::TournamentRegistrationParametersV5,
    ) -> Result<i32, Error> {
        let path = Cow::Borrowed("/lol/tournament/v5/tournaments");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let body = serde_json::to_string(body).map_err(Error::body_serialize)?;

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: Some(body),
            endpoint_id: "tournament-v5.registerTournament",
            method: "POST",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Update the pick type, map, spectator type, or allowed puuids for a code.
    ///
    /// # Riot API reference
    ///
    /// Operation `tournament-v5.updateCode`:
    /// <https://developer.riotgames.com/api-methods/#tournament-v5/PUT_updateCode>
    pub async fn tournament_v5_update_code(
        &self,
        route: RegionalRoute,
        tournament_code: &str,
        body: &super::models::tournament_v5::TournamentCodeUpdateParametersV5,
    ) -> Result<(), Error> {
        assert!(
            !tournament_code.is_empty(),
            "tournament code must not be empty"
        );

        let path = Cow::Owned(format!(
            "/lol/tournament/v5/codes/{}",
            path_encode(tournament_code)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let body = serde_json::to_string(body).map_err(Error::body_serialize)?;

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: Some(body),
            endpoint_id: "tournament-v5.updateCode",
            method: "PUT",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_unit(plan).await
    }

    /// Get match by id
    ///
    /// # Riot API reference
    ///
    /// Operation `val-console-match-v1.getMatch`:
    /// <https://developer.riotgames.com/api-methods/#val-console-match-v1/GET_getMatch>
    pub async fn val_console_match_v1_get_match(
        &self,
        route: ValPlatformRoute,
        match_id: &str,
    ) -> Result<Option<super::models::val_console_match_v1::MatchDto>, Error> {
        assert!(!match_id.is_empty(), "match id must not be empty");

        let path = Cow::Owned(format!(
            "/val/match/console/v1/matches/{}",
            path_encode(match_id)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "val-console-match-v1.getMatch",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get matchlist for games played by puuid and platform type
    ///
    /// # Riot API reference
    ///
    /// Operation `val-console-match-v1.getMatchlist`:
    /// <https://developer.riotgames.com/api-methods/#val-console-match-v1/GET_getMatchlist>
    pub async fn val_console_match_v1_get_matchlist(
        &self,
        route: ValPlatformRoute,
        puuid: &str,
        platform_type: &str,
    ) -> Result<super::models::val_console_match_v1::MatchlistDto, Error> {
        assert!(!puuid.is_empty(), "puuid must not be empty");
        assert!(!platform_type.is_empty(), "platform type must not be empty");

        let path = Cow::Owned(format!(
            "/val/match/console/v1/matchlists/by-puuid/{}",
            path_encode(puuid)
        ));

        let query_pairs: Vec<(&'static str, String)> =
            vec![("platformType", platform_type.to_string())];

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "val-console-match-v1.getMatchlist",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get recent matches
    /// ## Implementation Notes
    /// Returns a list of match ids that have completed in the last 10 minutes for live regions and
    /// 12 hours for the esports routing value. NA/LATAM/BR share a match history deployment. As
    /// such, recent matches will return a combined list of matches from those three regions.
    /// Requests are load balanced so you may see some inconsistencies as matches are added/removed
    /// from the list.
    ///
    /// # Riot API reference
    ///
    /// Operation `val-console-match-v1.getRecent`:
    /// <https://developer.riotgames.com/api-methods/#val-console-match-v1/GET_getRecent>
    pub async fn val_console_match_v1_get_recent(
        &self,
        route: ValPlatformRoute,
        queue: &str,
    ) -> Result<super::models::val_console_match_v1::RecentMatchesDto, Error> {
        assert!(!queue.is_empty(), "queue must not be empty");

        let path = Cow::Owned(format!(
            "/val/match/console/v1/recent-matches/by-queue/{}",
            path_encode(queue)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "val-console-match-v1.getRecent",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get leaderboard for the competitive queue
    ///
    /// # Riot API reference
    ///
    /// Operation `val-console-ranked-v1.getLeaderboard`:
    /// <https://developer.riotgames.com/api-methods/#val-console-ranked-v1/GET_getLeaderboard>
    pub async fn val_console_ranked_v1_get_leaderboard(
        &self,
        route: ValPlatformRoute,
        act_id: &str,
        platform_type: &str,
        start_index: Option<i32>,
        size: Option<i32>,
    ) -> Result<Option<super::models::val_console_ranked_v1::LeaderboardDto>, Error> {
        assert!(!act_id.is_empty(), "act id must not be empty");
        assert!(!platform_type.is_empty(), "platform type must not be empty");

        let path = Cow::Owned(format!(
            "/val/console/ranked/v1/leaderboards/by-act/{}",
            path_encode(act_id)
        ));

        let mut query_pairs: Vec<(&'static str, String)> =
            vec![("platformType", platform_type.to_string())];

        if let Some(value) = start_index {
            query_pairs.push(("startIndex", value.to_string()));
        }

        if let Some(value) = size {
            query_pairs.push(("size", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "val-console-ranked-v1.getLeaderboard",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get content optionally filtered by locale
    ///
    /// # Riot API reference
    ///
    /// Operation `val-content-v1.getContent`:
    /// <https://developer.riotgames.com/api-methods/#val-content-v1/GET_getContent>
    pub async fn val_content_v1_get_content(
        &self,
        route: ValPlatformRoute,
        locale: Option<&str>,
    ) -> Result<super::models::val_content_v1::ContentDto, Error> {
        let path = Cow::Borrowed("/val/content/v1/contents");

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(1);

        if let Some(value) = locale {
            query_pairs.push(("locale", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "val-content-v1.getContent",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get match by id
    ///
    /// # Riot API reference
    ///
    /// Operation `val-match-v1.getMatch`:
    /// <https://developer.riotgames.com/api-methods/#val-match-v1/GET_getMatch>
    pub async fn val_match_v1_get_match(
        &self,
        route: ValPlatformRoute,
        match_id: &str,
    ) -> Result<Option<super::models::val_match_v1::MatchDto>, Error> {
        assert!(!match_id.is_empty(), "match id must not be empty");

        let path = Cow::Owned(format!("/val/match/v1/matches/{}", path_encode(match_id)));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "val-match-v1.getMatch",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get matchlist for games played by puuid
    ///
    /// # Riot API reference
    ///
    /// Operation `val-match-v1.getMatchlist`:
    /// <https://developer.riotgames.com/api-methods/#val-match-v1/GET_getMatchlist>
    pub async fn val_match_v1_get_matchlist(
        &self,
        route: ValPlatformRoute,
        puuid: &str,
    ) -> Result<super::models::val_match_v1::MatchlistDto, Error> {
        assert!(!puuid.is_empty(), "puuid must not be empty");

        let path = Cow::Owned(format!(
            "/val/match/v1/matchlists/by-puuid/{}",
            path_encode(puuid)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "val-match-v1.getMatchlist",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get recent matches
    /// ## Implementation Notes
    /// Returns a list of match ids that have completed in the last 10 minutes for live regions and
    /// 12 hours for the esports routing value. NA/LATAM/BR share a match history deployment. As
    /// such, recent matches will return a combined list of matches from those three regions.
    /// Requests are load balanced so you may see some inconsistencies as matches are added/removed
    /// from the list.
    ///
    /// # Riot API reference
    ///
    /// Operation `val-match-v1.getRecent`:
    /// <https://developer.riotgames.com/api-methods/#val-match-v1/GET_getRecent>
    pub async fn val_match_v1_get_recent(
        &self,
        route: ValPlatformRoute,
        queue: &str,
    ) -> Result<super::models::val_match_v1::RecentMatchesDto, Error> {
        assert!(!queue.is_empty(), "queue must not be empty");

        let path = Cow::Owned(format!(
            "/val/match/v1/recent-matches/by-queue/{}",
            path_encode(queue)
        ));
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "val-match-v1.getRecent",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }

    /// Get leaderboard for the competitive queue
    ///
    /// # Riot API reference
    ///
    /// Operation `val-ranked-v1.getLeaderboard`:
    /// <https://developer.riotgames.com/api-methods/#val-ranked-v1/GET_getLeaderboard>
    pub async fn val_ranked_v1_get_leaderboard(
        &self,
        route: ValPlatformRoute,
        act_id: &str,
        size: Option<i32>,
        start_index: Option<i32>,
    ) -> Result<Option<super::models::val_ranked_v1::LeaderboardDto>, Error> {
        assert!(!act_id.is_empty(), "act id must not be empty");

        let path = Cow::Owned(format!(
            "/val/ranked/v1/leaderboards/by-act/{}",
            path_encode(act_id)
        ));

        let mut query_pairs: Vec<(&'static str, String)> = Vec::with_capacity(2);

        if let Some(value) = size {
            query_pairs.push(("size", value.to_string()));
        }

        if let Some(value) = start_index {
            query_pairs.push(("startIndex", value.to_string()));
        }

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "val-ranked-v1.getLeaderboard",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute_optional(plan).await
    }

    /// Get VALORANT status for the given platform.
    ///
    /// # Riot API reference
    ///
    /// Operation `val-status-v1.getPlatformData`:
    /// <https://developer.riotgames.com/api-methods/#val-status-v1/GET_getPlatformData>
    pub async fn val_status_v1_get_platform_data(
        &self,
        route: ValPlatformRoute,
    ) -> Result<super::models::val_status_v1::PlatformDataDto, Error> {
        let path = Cow::Borrowed("/val/status/v1/platform-data");
        let query_pairs: Vec<(&'static str, String)> = Vec::new();

        let plan = RequestPlan {
            auth: Auth::ApiKey,
            body: None,
            endpoint_id: "val-status-v1.getPlatformData",
            method: "GET",
            path,
            query: query_pairs,
            route: route.as_str(),
        };

        self.execute(plan).await
    }
}
